package com.froy.navigator.model;

/**
 * Enum representing the transport modes supported by the application.
 * Provides type safety and a single source of truth for the modes.
 */
public enum TransportMode {
    CAR,
    BIKE,
    MOTORCYCLE
}